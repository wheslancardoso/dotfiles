# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# User Settings - Zero Micro-Friction Edition

# Redimensionamento suave nas bordas sem exigir precisão de pixel
general {
  resize_on_border = true
  extend_border_grab_area = 15
  hover_icon_on_border = true
  layout = dwindle
}

# Foco segue o cursor do mouse instantaneamente (sem precisar clicar)
input {
  kb_layout = us
  kb_variant = intl
  kb_options = caps:swapescape
  follow_mouse = 1
  float_switch_override_focus = 2
  mouse_refocus = true
  sensitivity = 0
  touchpad {
    natural_scroll = true
    tap-to-click = true
    disable_while_typing = true
  }
}

# Foco ao abrir links/apps, swallow de janelas do terminal e economia de energia
misc {
  focus_on_activate = true
  mouse_move_enables_dpms = true
  vfr = true
  vrr = 1
  enable_swallow = true
  swallow_regex = ^(kitty|ghostty|alacritty)$
}

# Esconde o cursor após inatividade para não atrapalhar leitura/código
cursor {
  no_warps = true
  inactive_timeout = 3
  enable_hyprcursor = true
}

# Logitech K380 / K380s Bluetooth Keyboard explicit mapping
device {
  name = logitech-k380s-keyboard
  kb_layout = us
  kb_variant = intl
  kb_options = caps:swapescape
}

device {
  name = k380-keyboard
  kb_layout = us
  kb_variant = intl
  kb_options = caps:swapescape
}

# 🖱️ Shake to Find Cursor (Igual ao KDE Plasma / macOS)
# Chacoalhar o mouse faz o cursor crescer instantaneamente para achá-lo na tela
# hyprlang noerror true
plugin:dynamic-cursors {
  enabled = true
  mode = none

  shake {
    enabled = true
    threshold = 4.0
    base = 3.5
    speed = 4.0
    influence = 0.0
    limit = 5.0
    timeout = 800
    effects = false
  }

  hyprcursor {
    nearest = true
    enabled = true
    fallback = clientside
  }
}
# hyprlang noerror false

